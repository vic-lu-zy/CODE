[X,Y] = getXY(LogBin(1,:,:,:),cl,[1,3],1);
X = normalize2d(X,2);

c = cvpartition(size(X,1),'leaveout');
lambdas = [0.01, 0.03, 0.05, 0.1, 0.3, 0.5
rate = 1:50;
for ii = 1:50
fun = @(xT,yT,xt,yt)(sum(yt==LogisticRegression(xt,xT,yT,)));
result = crossval(fun,X,Y,'partition',c)>0;
rate = round(sum(result)/sum(c.TestSize)*100)